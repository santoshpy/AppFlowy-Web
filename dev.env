APPFLOWY_BASE_URL=http://localhost:8000
APPFLOWY_GOTRUE_BASE_URL=http://localhost:9999
APPFLOWY_WS_BASE_URL=ws://localhost:8000/ws/v2

# Brand identity (config-driven, see src/application/brand.ts). Defaults to AppFlowy when unset.
# Uncomment and edit to rebrand — no code changes required.
# APPFLOWY_BRAND_NAME=AppFlowy
# APPFLOWY_BRAND_DESCRIPTION=AppFlowy is an AI collaborative workspace where you achieve more without losing control of your data
# APPFLOWY_BRAND_URL=https://appflowy.com
# APPFLOWY_BRAND_TWITTER=@appflowy