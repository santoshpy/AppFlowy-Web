APPFLOWY_BASE_URL=http://localhost
APPFLOWY_GOTRUE_BASE_URL=http://localhost/gotrue
APPFLOWY_WS_BASE_URL=ws://localhost/ws/v2

# Brand identity (config-driven, see src/application/brand.ts). Defaults to Abhilekh when unset.
# Edit to re-deploy under a different identity — no code changes required.
APPFLOWY_BRAND_NAME=Abhilekh
APPFLOWY_BRAND_DESCRIPTION=Abhilekh — Nepal's national platform for managing documents and records across government departments
APPFLOWY_BRAND_URL=https://abhilekh.gov.np
APPFLOWY_BRAND_TWITTER=